/**
 * All environment variables referenced here so we have a
 * clear view of all variables required by the app and their
 * relevant defaults
 */
export const API = process.env.REACT_APP_API_URL ?? '/api';
